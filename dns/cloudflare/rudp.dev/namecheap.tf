# provider "namecheap" {
#   username    = "${var.nc-user}"
#   api_user    = "${var.nc-apiuser}"
#   token       = "${var.nc-apitoken}"
#   ip          = "${var.nc-ips}"
#   use_sandbox = "${var.nc-sandbox}"
# }

